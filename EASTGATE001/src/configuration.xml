<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties resource="db.properties" />
	<typeAliases>
		<typeAlias alias="admin" type="admin.Admin" />
		<typeAlias alias="member" type="member.Member" />
		<typeAlias alias="cart" type="cart.Cart" />
		<typeAlias alias="orders" type="orders.Orders" />
	</typeAliases>
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${driver}" />
				<property name="url" value="${url}" />
				<property name="username" value="${username}" />
				<property name="password" value="${password}" />
			</dataSource>
		</environment>
	</environments>
	<mappers>
		<mapper resource="Admin.xml" />
		<mapper resource="member.xml" />
		<mapper resource="cart.xml"/>
		<mapper resource="orders.xml"/>
	</mappers>
</configuration>